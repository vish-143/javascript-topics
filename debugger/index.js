function add(a, b) {
    return a + b;
  }
  
  function multiply(a, b) {
    return a * b;
  }
  
  function calculate() {
    let x = add(2, 3);
    let y = multiply(x, 4);
    console.log('Result:', y);
  }
  
  // Call the main function to trigger the debug
  calculate();
  